<ui_patterns>

面向用户的 GSD 输出的视觉模式。协调器通过 @ 引用此文件。

## 阶段横幅

用于主要工作流转换。

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 GSD ► {阶段名称}
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**阶段名称（大写）：**
- `QUESTIONING`（提问）
- `RESEARCHING`（研究）
- `DEFINING REQUIREMENTS`（定义需求）
- `CREATING ROADMAP`（创建路线图）
- `PLANNING PHASE {N}`（计划阶段 N）
- `EXECUTING WAVE {N}`（执行批次 N）
- `VERIFYING`（验证）
- `PHASE {N} COMPLETE ✓`（阶段 N 完成）
- `MILESTONE COMPLETE 🎉`（里程碑完成）

---

## 检查点框

需要用户操作。62 字符宽度。

```
╔══════════════════════════════════════════════════════════════╗
║  CHECKPOINT: {类型}                                          ║
╚══════════════════════════════════════════════════════════════╝

{内容}

──────────────────────────────────────────────────────────────
→ {操作提示}
──────────────────────────────────────────────────────────────
```

**类型：**
- `CHECKPOINT: Verification Required` → `→ 输入 "approved" 或描述问题`
- `CHECKPOINT: Decision Required` → `→ 选择：option-a / option-b`
- `CHECKPOINT: Action Required` → `→ 完成后输入 "done"`

---

## 状态符号

```
✓  完成 / 通过 / 已验证
✗  失败 / 缺失 / 受阻
◆  进行中
○  待处理
⚡ 自动批准
⚠  警告
🎉 里程碑完成（仅用于横幅）
```

---

## 进度显示

**阶段/里程碑级别：**
```
进度：████████░░ 80%
```

**任务级别：**
```
任务：2/4 完成
```

**计划级别：**
```
计划：3/5 完成
```

---

## 生成指示器

```
◆ 正在生成研究者...

◆ 正在并行生成 4 个研究者...
  → 栈研究
  → 功能研究
  → 架构研究
  → 陷阱研究

✓ 研究者完成：已写入 STACK.md
```

---

## 接下来区块

始终在主要完成结束时。

```
───────────────────────────────────────────────────────────────

## ▶ 接下来

**{标识符}：{名称}** —— {单行描述}

`{复制粘贴命令}

<sub>`/clear` 先执行 → 清空上下文窗口</sub>

───────────────────────────────────────────────────────────────

**也可用：**
- `/gsd:alternative-1` —— 描述
- `/gsd:alternative-2` —— 描述

───────────────────────────────────────────────────────────────
```

---

## 错误框

```
╔══════════════════════════════════════════════════════════════╗
║  错误                                                       ║
╚══════════════════════════════════════════════════════════════╝

{错误描述}

**修复方法：**{解决步骤}
```

---

## 表格

```
| 阶段 | 状态 | 计划 | 进度 |
|-------|--------|-------|----------|
| 1     | ✓      | 3/3   | 100%     |
| 2     | ◆      | 1/4   | 25%      |
| 3     | ○      | 0/2   | 0%       |
```

---

## 反模式

- 变化的框/横幅宽度
- 混合横幅样式（`===`、`---`、`***`）
- 横幅中跳过 `GSD ►` 前缀
- 随机表情符号（`🚀`、`✨`、`💫`）
- 完成后缺少接下来区块

</ui_patterns>
